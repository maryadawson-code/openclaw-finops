# Root wrangler.toml — points to the FinOps app for backward compatibility.
# For per-app deployment, cd into apps/finops or apps/api-bridge.
name = "integritypulse"
main = "apps/finops/src/index.ts"
compatibility_date = "2024-12-01"
compatibility_flags = ["nodejs_compat"]
