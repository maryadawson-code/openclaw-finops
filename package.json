{
  "name": "integritypulse-suite",
  "version": "1.0.0",
  "private": true,
  "description": "IntegrityPulse Suite — Revenue-gated MCP servers for AI agents",
  "type": "module",
  "scripts": {
    "dev": "pnpm --filter @integritypulse/finops dev",
    "dev:finops": "pnpm --filter @integritypulse/finops dev",
    "dev:api-bridge": "pnpm --filter @integritypulse/api-bridge dev",
    "deploy:finops": "pnpm --filter @integritypulse/finops deploy",
    "deploy:api-bridge": "pnpm --filter @integritypulse/api-bridge deploy",
    "dev:guardrail": "pnpm --filter @integritypulse/guardrail dev",
    "dev:fortress": "pnpm --filter @integritypulse/fortress dev",
    "deploy:fortress": "pnpm --filter @integritypulse/fortress deploy",
    "deploy:guardrail": "pnpm --filter @integritypulse/guardrail deploy",
    "dev:ambassador": "pnpm --filter @integritypulse/ambassador dev",
    "deploy:ambassador": "pnpm --filter @integritypulse/ambassador deploy",
    "deploy:all": "pnpm -r deploy",
    "typecheck": "pnpm -r typecheck"
  },
  "devDependencies": {
    "@types/node": "^25.5.0",
    "dotenv": "^17.3.1",
    "tsx": "^4.21.0",
    "turbo": "^2.5.0"
  }
}
